import 'package:flutter/material.dart';

import 'components/backGroundStart.dart';

class NoticiasFragment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return new Scaffold(
      body: BackStart()
    );
  }
}